import { styled } from "@mui/system";
import Button from '@mui/material/Button';

const SpecialButton = styled(Button)({
    background: 'linear-gradient(  rgba(124, 62, 4, 0.643) , rgba(124, 62, 4, 0.849)  )',
    color: '#fff',
    padding: '10px 22px'
})

export default SpecialButton;